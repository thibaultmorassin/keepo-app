Rounded surface. `hero` for the one summary block at the top of a screen, `card` for everything else.

```jsx
<Card tone="brand" size="hero"><CoverageBar pct={70} /></Card>
```

At most one `brand` card per screen — it is the screen's anchor.
