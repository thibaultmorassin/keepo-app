Toggle chip for filters and category pickers. Unselected is a hairline outline, never a filled grey.

```jsx
<Chip tone="neutral" selected={filter === 'Bientôt'} onClick={() => setFilter('Bientôt')}>Bientôt</Chip>
```
