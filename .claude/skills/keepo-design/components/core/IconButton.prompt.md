Circular icon-only control for navigation chrome (back, close, overflow).

```jsx
<IconButton label="Retour" onClick={back}><ArrowLeft /></IconButton>
```

`inverse` is for use over the camera view and the brand hero; `brand` for a tinted affordance.
