Renders a single Lucide glyph at the Keepo stroke weight (2.25).

```jsx
<Icon name="shield-check" size={22} />
```

Requires the Lucide UMD script on the page. Never hand-draw an SVG path in product code — pick a Lucide name instead.
