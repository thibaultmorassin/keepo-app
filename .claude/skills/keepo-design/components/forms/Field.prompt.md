Labelled input. Placeholder shows a realistic example, never a restatement of the label.

```jsx
<Field
  label="Magasin"
  value={store}
  onChange={setStore}
  placeholder="Darty Beaugrenelle"
/>
```

Never use a floating label — Keepo keeps labels permanently visible so a half-filled form is still readable.
