Tappable radio row, pill-shaped, 44px minimum. Selection tints the whole row green.

```jsx
<OptionRow label="S'arrête en cours de cycle" selected={issue === 'cycle'} onSelect={() => setIssue('cycle')} />
```
