Empty state. Never ends on a dead end — always pass an `action`.

```jsx
<EmptyState title="Rien à surveiller ici" body="Ajoutez votre premier achat, on s'occupe des dates."
            action={<Button>Ajouter un objet</Button>} />
```
